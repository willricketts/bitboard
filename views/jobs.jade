extends layouts/default

block content
  h2 List of jobs

  each job in jobs
    a.ui.card.fluid.link(href="/jobs/#{job._id}")
      .content
        h3.header #{job.title}
        h5.header Posted on #{job.created}
        p.description #{job.content}
